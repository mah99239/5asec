<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <Button
        android:id="@+id/btn_rail_header_cart"
        style="@style/Widget.5asec.iconButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:icon="@drawable/ic_bar_shopping" />

    <Button
        android:id="@+id/btn_rail_header_notifaction"
        style="@style/Widget.5asec.iconButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:contentDescription="@null"

        app:layout_constraintEnd_toEndOf="@+id/btn_rail_header_cart"
        app:layout_constraintStart_toStartOf="@+id/btn_rail_header_cart"
        app:layout_constraintTop_toBottomOf="@+id/btn_rail_header_cart"
        app:icon="@drawable/ic_bar_notifaction" />
</androidx.constraintlayout.widget.ConstraintLayout>