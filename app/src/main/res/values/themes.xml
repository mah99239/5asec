<resources xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
  <!-- Base application theme. -->
  <style name="Theme.5asec" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
    <!-- Primary brand color. -->
    <item name="colorPrimary">@color/primaryColor</item>
    <item name="colorPrimaryVariant">@color/primaryLightColor</item>
    <item name="colorOnPrimary">@color/primaryTextColor</item>
    <!-- Secondary brand color. -->
    <item name="colorSecondary">@color/secondaryColor</item>
    <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
    <item name="colorOnSecondary">@color/primaryTextColor</item>
    <!-- Status bar color. -->
    <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    <!-- Customize your theme here. -->

    <!-- style used in the MainActivity and SignUpActivity for values of Button -->
  </style>
  <style name="buttonSmall_main" parent="Widget.AppCompat.Button.Small">
    <item name="android:layout_width">0dp</item>
    <item name="android:layout_height">@dimen/button_main_size</item>
    <item name="android:insetTop">0dp</item>
    <item name="android:insetBottom">0dp</item>
  </style>

  <!-- Style used in the MainActivity and SignUp for values of EditText-->
  <style name="editText_main_check" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
    <item name="android:layout_width">0dp</item>
    <item name="android:layout_height">wrap_content</item>
    <item name="boxBackgroundColor">@color/primaryTextColor</item>
    <item name="errorEnabled">true</item>
    <item name="helperTextEnabled">true</item>
    <item name="helperTextTextColor">@color/error</item>
    <item name="boxStrokeColor">@color/error</item>

    <item name="android:layout_marginTop">@dimen/view_main_margin </item>
  </style>

    <style name="SpinnerDatePickerDialog" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
      <item name="android:datePickerStyle">@style/SpinnerDatePicker</item>
      <item name="android:background">?attr/colorBackgroundFloating</item>
      <item name="android:dialogCornerRadius" tools:targetApi="28">16dp</item>
      <item name="android:gravity">bottom</item>
    </style>

    <style name="SpinnerDatePicker" parent="@android:style/Widget.DatePicker">
      <item name="android:calendarViewShown">false</item>
      <item name="android:datePickerMode" tools:targetApi="lollipop">spinner</item>
      <item name="android:layout_gravity">bottom</item>
      <item name="android:gravity">bottom</item>

    </style>


</resources>