package com.example.a5asec.data.model.db;

import androidx.room.TypeConverter;

import com.google.gson.Gson;
import com.google.gson.reflect.TypeToken;

import java.lang.reflect.Type;
import java.util.List;

public class Converters
    {
 /*   @TypeConverter
    public static String fromItemsList(List<DatabaseCategory.ItemsEntity> items) {
    Gson gson = new Gson();
    Type type = new TypeToken<List<DatabaseCategory.ItemsEntity>>() {}.getType();
    return gson.toJson(items, type);
    }

    @TypeConverter
    public static List<DatabaseCategory.ItemsEntity> toItemsList(String itemsString) {
    Gson gson = new Gson();
    Type type = new TypeToken<List<DatabaseCategory.ItemsEntity>>() {}.getType();
    return gson.fromJson(itemsString, type);
    }*/
    }
