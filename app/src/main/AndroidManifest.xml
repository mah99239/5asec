<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.a5asec">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.5asec"
      tools:targetApi="m">
    <activity android:name=".ui.main.view.MainActivity2"></activity>
    <activity android:name=".ui.main.view.SplashActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".ui.main.view.SuccessActivity" />
    <activity
        android:name=".ui.main.view.TermsActivity"
        android:parentActivityName=".ui.main.view.SignUpActivity" />
    <activity android:name=".ui.main.view.MainActivity"></activity>
    <activity
        android:name=".ui.main.view.SignUpActivity"
        android:parentActivityName=".ui.main.view.MainActivity" />
  </application>
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <uses-permission android:name="android.permission.INTERNET" />

</manifest>